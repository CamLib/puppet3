---
:backends:
    - eyaml
    - yaml
    - module_data

:hierarchy:
    - "%{clientcert}"
    - "%{operatingsystem}/%{operatingsystemrelease}"
    - "%{environment}"
    - "common"

:yaml:
    :datadir: '/usr/local/etc/puppet/data'

:eyaml:
    :datadir: '/usr/local/etc/puppet/data'

